#ifndef MPAGSCIPHER_CIPHERMODE_HPP
#define MPAGSCIPHER_CIPHERMODE_HPP
/// enum Ciphermode can be either 'encrypt' or 'decrypt' and is used as similarly to a boolean to decide to encrypt or decrypt data.
enum class CipherMode {
	encrypt,
	decrypt
};
#endif
